import { validateRegisterAPI } from "@/modules/registration/server/validateRegisterAPI";

/* const ValidateRegistration: NextApiHandler<any> = async (req, res) => {
  const response = await validateRegisterAPI(req, res);
  return response;
}; */
export default validateRegisterAPI;
